extends "res://scripts/objective_hint_base.gd"
## 일상 파트(챕터마다 공유하는 허브 `chapter1_real.tscn`)용 목표 힌트
## (사람 피드백, 2026-09-15 — "일상 단계에서도 뭘 해야 다음 스텝으로
## 넘어가는지 알려주도록 하자". 지금까지는 챕터 1 꿈에만 힌트가 있었음).
##
## 일상 파트에서 다음으로 넘어가는 유일한 수단은 낮잠(`NapTrigger`)
## 뿐이라 새 스토리 텍스트 없이 항상 같은 문구를 보여준다 — 마지막
## 챕터(3장)를 끝내면 이 씬으로 다시 돌아오지 않으므로 "더 할 게 없음"
## 상태를 따로 처리할 필요가 없다.
##
## 화면 하단 페이드/숨김 공통 로직은 `objective_hint_base.gd` 참고.

func _current_hint() -> String:
	return "낮잠을 자면 다음 이야기로 이어질 것 같다."
