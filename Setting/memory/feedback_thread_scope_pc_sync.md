---
name: feedback-thread-scope-pc-sync
description: "This conversation thread (in D:\\Claude\\ProjectS) is reserved by the user for cross-PC work-transfer methods and running work-transfer commands only."
metadata:
  type: feedback
  modified: 2026-09-04T00:00:00.000Z
---

The user declared (2026-09-04) that going forward, this specific thread is dedicated to
**cross-PC work-transfer**: discussing how to move ProjectS's code/config between PCs, and
running the actual transfer commands (git push/pull, `Setting/sync-to-repo.ps1`,
`Setting/sync-from-repo.ps1`, etc. — see [[project_projectd_loop_ops]] project's sibling
setup, the `Setting/` sync system documented in this repo's `CLAUDE.md`).

**Why**: the user wants a clean, single-purpose channel for sync operations, separate from
other ProjectS feature/dev work (which presumably happens in other threads/sessions).

**How to apply**: in this thread, stay focused on PC-sync topics (uploading to GitHub,
restoring environment on a new PC, adjusting the `Setting/` sync scripts or the CLAUDE.md
workflow rules). If the user asks for unrelated ProjectS feature work here, it's fine to do
it if they explicitly ask, but don't proactively pull in other project work.
