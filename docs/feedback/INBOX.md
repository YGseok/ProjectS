# INBOX.md — 사용자 지시 / 피드백 (최우선)

이 파일은 사람이 직접 채워 넣는 지시함이다. 루프의 모든 이터레이션은
`docs/DESIGN.md`, `docs/STATUS.md`를 읽은 직후 **이 파일을 반드시 읽고,
여기 있는 항목을 다른 어떤 작업보다 먼저 처리한다.**

## 사용법 (사람용)

- 새 지시/피드백은 **맨 위에** 날짜와 함께 추가한다 (최신이 위).
- 처리가 끝난 항목은 에이전트가 `[처리됨]` 표시를 남기고 아래
  "처리된 항목" 섹션으로 옮긴다. 사람은 지우지 않아도 된다.
- 급한 지시는 `[긴급]` 태그를 붙인다.


## 남은 이슈 (미처리)

(플레이해보고 느낀 점을 이 섹션에 자유롭게 적어주세요.)
- (2026-09-09) 오프닝 다이얼로그가 자동 재생되지 않도록 한다.
- (2026-09-09) 퍼즐 방식을 바꾼다. 꿈을 오가는게 아니라, 꿈 안에서 무언가를 해야 탈출하는 구조로 바꾼다.
- (2026-09-09) 임의의 퍼즐이 포함된 챕터 2개를 추가로 만든다. 플로우도 1챕터와 동일하게 꿈 진입 -> 퍼즐 해결 -> 꿈 탈출이다.
- (2026-09-09) 챕터의 꿈에 진입하면 챕터가 시작되고, 꿈에 탈출하면 챕터가 종료된다. 챕터 종료한 뒤 일상이 시작된다. 첫 꿈 들어가기 이전은 프롤로그. 첫 꿈 들어간 후 1챕터 타이틀이 뜨고, 퍼즐을 진행한다. 첫 꿈 탈출하면, 1챕터 완료 타이틀이 뜬다. 
- (2026-09-09) 탈출한 후, 바깥을 돌아다니며 NPC 와 대화하거나 단서를 얻을 수 있는 일상 플레이가 가능하다.
- (2026-09-09) 각 챕터 앞뒤로 점프할 수 있도록, 개발자용 챕터 이동 치트를 만든다. 모든 상황에서 접근 및 스킵이 가능하면 좋을 것 같다. 예를 들자면 프롤로그/1/1종료/2/2종료/... 이동하면 현재 진행도를 날리고, 해당 진행도 시작지점에 보유하고 있어야하는 아이템만 지니게 한다.
- (2026-09-08) 좀더 돌아다니는 느낌이 있도록, 맵을 확장해야할 것 같다. 집도 돌아다닐 수 있을만큼 넓고, 방도 두 개 정도 더 만들자.
- (2026-09-08) 맵이 과하게 넓어 화면이 넓은 경우, 화면 끝에 가기 전에 스크롤링 되어야 한다. 쯔꾸르 게임에서의 캐릭터 이동 방식과 맵 스크롤링을 참고한다.
- (2026-09-08) 각 움직임 및 퍼즐 다듬은 후, 게임을 만들어야 함. 지금은 재미가 없음. 인터렉션 오브젝트나 풋 스위치 등. 무언가가 더 필요하다.

## 처리 대기 중

- (2026-09-08) 간단한 도트 형태의 미니맵이 필요할지는 현재 고민 중이다.
- (2026-09-01) 네모 박스 대신 플레이어블(주인공) 캐릭터를 디자인한다. 캐릭터는
  흰 원피스를 입은 10살 전후의 한국인 여아로 설정한다. 캐릭터 디자인을 먼저
  컨펌받은 후, 도트 스프라이트를 제작하여 저장한다. (디자인은 거듭 고쳐질 수
  있으나, 본 목적은 반복된 플레이에서 이미지 변경이 없도록 확정하기 위함)
  **[진행 중 — 미완료]** 절차적 생성(v1~v7)으로 시도했으나 진짜 이미지 생성이
  아니라 도형 조합이라 한계가 있어 이 방식은 중단(2026-09-04). 이후 받은
  `assets/sprites/characters/bonus_pack/` 캐릭터들도 이 디자인(흰 원피스,
  10살 한국인 여아)과 맞지 않아 아직 확정된 스프라이트 없음. 맞는 에셋을
  더 구하거나 별도 제작 필요 — 계속 대기.

## 처리된 항목

- [처리됨] (2026-09-09) NPC 및 대화 가능한 오브젝트에도 벽처럼 충돌
  판정을 추가한다. `interactable_base.gd`(판자/공기돌/사방치기/장독/
  나무·덤불 조사)와 `npc.gd`를 "blocks_movement" 그룹에 넣고,
  `collision_map.gd`가 그 그룹의 노드 중 지금 보이는(=등장한) 것만
  타일 하나만큼 막도록 함 — 아직 등장 안 한 단계의 사방치기/장독은
  안 보이는 동안엔 그대로 통과되다가 등장하는 순간부터 막힌다(수동
  동기화 불필요). 상호작용은 이제 옆 칸에서 해야 해서 interact_radius
  기본값도 28→34로 올림. 나무/덤불은 이미 전용 모양의 콜리전이 있어서
  이 동적 검사에서는 제외(안 그러면 트렁크와 안 맞는 칸이 막힘).
  자세한 내용은 STATUS.md v0.12 참고.
- [처리됨] (2026-09-08) 어디서 왜 무엇을 해야하는지 인지하기 어려움 —
  목표 힌트 UI 추가. `scenes/common/ObjectiveHint.tscn` +
  `scripts/objective_hint.gd`를 `chapter1_real.tscn` 화면 하단에 배치,
  `Chapter1Progress`의 기계적 상태(stage/has_key/has_stamp/
  diary_opened)만으로 "지금 뭘 해야 하는지" 한 줄을 상시 보여줌(예:
  "마루 밑에서 뭔가 이상한 낌새가 느껴진다." → 열쇠 획득 후 "자물쇠에
  맞는 게 하나 더 필요할 것 같다." → ...). **새 스토리 내용은 만들지
  않음** — 각 오브젝트가 이미 대사로 드러내는 정보를 요약해서 보여주는
  것뿐. 처음엔 화면 상단에 뒀다가 "기와집" 라벨과 겹치는 레이아웃
  버그를 QA 캡처로 발견해서 하단(꿈 씬의 MoodLabel과 같은 자리)으로
  옮김. `test_chapter1_puzzle.gd`에 단계별로 힌트가 바뀌는지, 일기
  개봉 후 비워지는지 확인하는 어서션 3개 추가.
- [처리됨] (2026-09-08) 프롤로그 필요 — 자동 재생 + 스킵 가능한 다이얼로그.
  `scenes/chapter1_intro.tscn` + `scripts/intro_sequence.gd` 신설,
  `project.godot`의 `run/main_scene`을 이 씬으로 바꿔서 실제 게임
  시작점으로 만듦. 한 줄이 3초 뒤 자동으로 넘어가거나(자동 재생),
  Enter로 그 줄만 즉시 스킵, Esc로 전체를 한 번에 건너뛰고 바로
  `chapter1_real.tscn`으로 감. **텍스트는 임시 자리표시**다 —
  DESIGN.md §8에 이미 확정된 사실(10세 여자아이, 매년 여름 시골 옛집
  방문, 가족과 함께, 도착한 날 낮잠)만 썼고, 실제 대사/연출은 스토리
  세션에서 확정 후 교체 필요. `tests/test_intro_sequence.gd`로 자동
  재생/한 줄 스킵/전체 스킵/전환까지 검증(4개 어서션).
- [처리됨] (2026-09-08) 획득한 아이템이 좌상단에 쌓이고, 다 쓰면 사라져야
  함 — 인벤토리 UI 구현. `scenes/common/InventoryUI.tscn` +
  `scripts/inventory_ui.gd`를 `chapter1_real.tscn`에 배치, 화면 좌상단에
  열쇠/나무패 슬롯을 두고 `Chapter1Progress.has_key`/`has_stamp`에 따라
  각각 보이거나 숨는다. 일기 개봉(`diary_opened`) 시점에 두 아이템이
  동시에 "다 쓰임" 처리되어 슬롯이 한꺼번에 사라짐(지시의 "다시 쓰일 일
  없는 아이템이라면 제거" 부분). **부분 처리**: 지시에 있던 "스토리라인상
  유의미한 아이템도 레거시로 남긴다"는 챕터 1에 그런 종류의 아이템이
  아직 없어서(열쇠/나무패 둘 다 일회성 퍼즐 소모품) 적용할 대상이 없음 —
  나중에 그런 아이템이 생기면 다시 볼 것. `test_chapter1_puzzle.gd`에
  슬롯 등장/유지/소진 검증 어서션 4개 추가.
- [처리됨] (2026-09-08) 아이템 획득 피드백 부족 — 아이템 획득 팝업 구현.
  새 오토로드 `ItemPopup`(`scripts/item_popup.gd` +
  `scenes/common/ItemPopup.tscn`)이 아이콘+이름을 화면 중앙에 띄우고
  `ui_accept`(Enter/Space) 한 번으로 닫힌다. `hopscotch_key.gd`/
  `jar_stamp.gd`가 열쇠/나무패를 처음 획득할 때 기존 플레이버 대사가
  끝난 직후 자동으로 팝업을 띄우도록 연결. 아이콘은 아직 자리표시용
  단색 사각형(`tools/gen_item_icons.gd`로 생성, `assets/props/
  ui_icons/`)이고, 팝업이 떠 있는 동안은 `player.gd`/
  `interactable_base.gd`가 이동/재상호작용을 막는다(대화창 활성 중
  체크와 같은 패턴). `tests/test_chapter1_puzzle.gd`에 팝업 등장/내용/
  닫힘까지 검증하는 어서션 6개 추가. **남은 부분**: 인벤토리 UI(좌상단
  상시 표시, 다음 이슈 항목)는 아직 별개 — 이번엔 "획득 순간의 팝업
  피드백"만 구현.
- [처리됨] (2026-09-07) 챕터 1 메인 퍼즐 확정 — "닫힌 일기장" (§8.1 교체).
  `docs/DESIGN.md` §8.1을 지시대로 4단계 수집 퍼즐로 다시 쓰고, 이후
  실제 오브젝트 구현까지 완료(마루 밑 판자/공기돌/사방치기 열쇠/장독
  나무패, `Chapter1Progress` 오토로드로 순환마다 단계 진행,
  `tests/test_chapter1_puzzle.gd`로 전 단계 자동 검증). "일기 개봉이
  각성(챕터 종료)을 유발"하는 트리거도 이어서 구현 완료 — 꿈의
  WakeTrigger를 게이팅하는 대신, 일기 대사가 끝나는 순간
  `DialogueSystem.dialogue_ended` 신호로 자동으로 새 임시 종료 화면
  (`scenes/chapter1_end.tscn`)으로 전환하도록 분리 구현(WakeTrigger는
  1~3단계 진행용 역할 그대로 유지). 자세한 내용은 STATUS.md 완료 기록
  참고. **남은 부분**: 오브젝트 아트는 전부 `ColorRect` 그레이박스 상태,
  `chapter1_end.tscn`은 챕터 2가 없어서 의도적으로 텍스트 한 줄짜리
  최소 화면.
- [처리됨] (2026-09-01) 대화 시스템을 만든다 — `scripts/dialogue_system.gd` +
  `scenes/common/DialogueSystem.tscn`(전역 오토로드)로 구현. 하단 대화창,
  `ui_accept`(Enter/Space)로 진행/종료. `tests/test_dialogue_interaction.gd`
  로 자동 검증까지 완료 (2026-09-04).
- [처리됨] (2026-09-01) 대화 시스템 테스트 환경 — `scripts/npc.gd` +
  `scenes/common/ShadowNPC.tscn`로 재사용 가능한 NPC 컴포넌트 구현,
  `chapter1_real.tscn` 툇마루에 배치. 대화 시 "[...]"만 출력 확인 (2026-09-04).
- [처리됨] (2026-09-01) 배경 디자인 — RPG Maker 스타일 타일셋 팩
  (`assets/tiles/main/`, 시트 형태로 저장, Godot TileSet이 자동으로 타일
  단위 분할)을 받아 `chapter1_real.tscn`/`chapter1_dream.tscn`의 그레이박스를
  실제 타일로 교체 완료 (2026-09-07). 지붕/벽 디테일은 계속 다듬는 중.
