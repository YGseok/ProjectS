# INBOX.md — 사용자 지시 / 피드백 (최우선)

이 파일은 사람이 직접 채워 넣는 지시함이다. 루프의 모든 이터레이션은
`docs/DESIGN.md`, `docs/STATUS.md`를 읽은 직후 **이 파일을 반드시 읽고,
여기 있는 항목을 다른 어떤 작업보다 먼저 처리한다.**

## 사용법 (사람용)

- 새 지시/피드백은 **맨 위에** 날짜와 함께 추가한다 (최신이 위).
- 처리가 끝난 항목은 에이전트가 `[처리됨]` 표시를 남기고 아래
  "처리된 항목" 섹션으로 옮긴다. 사람은 지우지 않아도 된다.
- 급한 지시는 `[긴급]` 태그를 붙인다.


## 남은 이슈 (미처리)

(플레이해보고 느낀 점을 이 섹션에 자유롭게 적어주세요.)
- (2026-09-08) 내가 누구고 어떤 상황에 놓여있는지, 프롤로그가 필요함. 실질적인 플레이보다는 자동 재생 및 다이얼로그 위주. 해당 다이얼로그는 특정 버튼으로 스킵이 가능해야 함.
- (2026-09-08) 아이템 획득 피드백이 부족함. 아이템 이미지, 이름과 함께 획득 팝업이 떠야함. 키 입력을 하면 닫힘.
- (2026-09-08) 내가 획득한 아이템은 적절한 위치(좌상단?)에 쌓여야함. 획득했고, 써야할 아이템임을 인지할 수 있도록. 이후 이벤트에서 다시 쓰일 일 없는 아이템이라면 제거. 스토리라인상 유의미한 아이템도 레거시로 남긴다.
- (2026-09-08) 어디에서 왜 무엇을 해야하는지 인지하기 어려움. 좀더 게임같이 개연성있게 연결시켜보자.
- (2026-09-08) 좀더 돌아다니는 느낌이 있도록, 맵을 확장해야할 것 같다. 집도 돌아다닐 수 있을만큼 넓고, 방도 두 개 정도 더 만들자.
- (2026-09-08) 맵이 과하게 넓어 화면이 넓은 경우, 화면 끝에 가기 전에 스크롤링 되어야 한다. 쯔꾸르 게임에서의 캐릭터 이동 방식과 맵 스크롤링을 참고한다.
- (2026-09-08) 각 움직임 및 퍼즐 다듬은 후, 게임을 만들어야 함. 지금은 재미가 없음. 인터렉션 오브젝트나 풋 스위치 등. 무언가가 더 필요하다.

## 처리 대기 중

- (2026-09-08) 간단한 도트 형태의 미니맵이 필요할지는 현재 고민 중이다.
- (2026-09-01) 네모 박스 대신 플레이어블(주인공) 캐릭터를 디자인한다. 캐릭터는
  흰 원피스를 입은 10살 전후의 한국인 여아로 설정한다. 캐릭터 디자인을 먼저
  컨펌받은 후, 도트 스프라이트를 제작하여 저장한다. (디자인은 거듭 고쳐질 수
  있으나, 본 목적은 반복된 플레이에서 이미지 변경이 없도록 확정하기 위함)
  **[진행 중 — 미완료]** 절차적 생성(v1~v7)으로 시도했으나 진짜 이미지 생성이
  아니라 도형 조합이라 한계가 있어 이 방식은 중단(2026-09-04). 이후 받은
  `assets/sprites/characters/bonus_pack/` 캐릭터들도 이 디자인(흰 원피스,
  10살 한국인 여아)과 맞지 않아 아직 확정된 스프라이트 없음. 맞는 에셋을
  더 구하거나 별도 제작 필요 — 계속 대기.

## 처리된 항목

- [처리됨] (2026-09-07) 챕터 1 메인 퍼즐 확정 — "닫힌 일기장" (§8.1 교체).
  `docs/DESIGN.md` §8.1을 지시대로 4단계 수집 퍼즐로 다시 쓰고, 이후
  실제 오브젝트 구현까지 완료(마루 밑 판자/공기돌/사방치기 열쇠/장독
  나무패, `Chapter1Progress` 오토로드로 순환마다 단계 진행,
  `tests/test_chapter1_puzzle.gd`로 전 단계 자동 검증). "일기 개봉이
  각성(챕터 종료)을 유발"하는 트리거도 이어서 구현 완료 — 꿈의
  WakeTrigger를 게이팅하는 대신, 일기 대사가 끝나는 순간
  `DialogueSystem.dialogue_ended` 신호로 자동으로 새 임시 종료 화면
  (`scenes/chapter1_end.tscn`)으로 전환하도록 분리 구현(WakeTrigger는
  1~3단계 진행용 역할 그대로 유지). 자세한 내용은 STATUS.md 완료 기록
  참고. **남은 부분**: 오브젝트 아트는 전부 `ColorRect` 그레이박스 상태,
  `chapter1_end.tscn`은 챕터 2가 없어서 의도적으로 텍스트 한 줄짜리
  최소 화면.
- [처리됨] (2026-09-01) 대화 시스템을 만든다 — `scripts/dialogue_system.gd` +
  `scenes/common/DialogueSystem.tscn`(전역 오토로드)로 구현. 하단 대화창,
  `ui_accept`(Enter/Space)로 진행/종료. `tests/test_dialogue_interaction.gd`
  로 자동 검증까지 완료 (2026-09-04).
- [처리됨] (2026-09-01) 대화 시스템 테스트 환경 — `scripts/npc.gd` +
  `scenes/common/ShadowNPC.tscn`로 재사용 가능한 NPC 컴포넌트 구현,
  `chapter1_real.tscn` 툇마루에 배치. 대화 시 "[...]"만 출력 확인 (2026-09-04).
- [처리됨] (2026-09-01) 배경 디자인 — RPG Maker 스타일 타일셋 팩
  (`assets/tiles/main/`, 시트 형태로 저장, Godot TileSet이 자동으로 타일
  단위 분할)을 받아 `chapter1_real.tscn`/`chapter1_dream.tscn`의 그레이박스를
  실제 타일로 교체 완료 (2026-09-07). 지붕/벽 디테일은 계속 다듬는 중.
